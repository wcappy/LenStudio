/**
 * Animation effect — identity transform.
 * Frames are played in sequence order (same as upload order).
 */
export function processAnimation(frames: ImageData[]): ImageData[] {
  return frames;
}
